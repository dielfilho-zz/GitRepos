import React from 'react';

interface GitContainerProps {
    row?: boolean;
    justifyContent?: string;
    alignItems?: string;
    marginTop?: string;
    children: React.ReactNode;
}

export default GitContainerProps;