import styled from 'styled-components';

const Input = styled.input`
  padding: 0.5em;
  margin: 0.5em;
  color: blue;
  width: 20%;
  border: 1px bold;
  border-color: grey;
  border-radius: 3px;
`;

export default Input;